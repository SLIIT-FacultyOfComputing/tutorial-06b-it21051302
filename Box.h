class Box 
{
    private:
       int length;
       int width;
       int height;
    public:
       // write prototypes of setters for length, width and height
       void setLenght(int 1);
       void setwidth(int w);
       void setheigth(int h);


       // write prototypes of getters for length, width and height
       void setLenght();
       void setwidth();
       void setheigth();
 
       int calcVolume();
};
